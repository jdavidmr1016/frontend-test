import { createSlice } from "@reduxjs/toolkit";

const initialState = [];

export const msjSlice = createSlice({
  name: "auth",
  initialState,
  reducers: {},
});
export const {} = msjSlice.actions;
export default msjSlice.reducer;
